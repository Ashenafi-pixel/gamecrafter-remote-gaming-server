# Env and secrets
.env
.env.local
*.pem

# Go (binaries only; /server is root-level binary, not server/ package)
/server
/gamecrafter
!/server/
*.exe
*.test
vendor/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Data (if local)
data/
