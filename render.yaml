# Render Blueprint - RGS backend with Supabase
# Docs: https://render.com/docs/blueprint-spec
# Root Directory: leave empty (repo root).
services:
  - type: web
    name: latam-rgs-backend
    runtime: go
    region: frankfurt
    plan: free

    # Output "app" to match Render's default; chmod fixes "Permission denied" on free tier
    buildCommand: go build -o app ./cmd/server
    startCommand: chmod +x app && ./app

    # Set in Render Dashboard → Environment (sync: false = you provide values)
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: OPERATOR_SECRET
        sync: false
    # PORT is set automatically by Render; config prefers PORT then RGS_PORT
